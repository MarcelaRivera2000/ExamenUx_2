import React from 'react'

const Btn=({amigo,user})=>{
        const Validacion=()=>{
            var aux=user.amigo;
            for (let i = 0; i < aux.length; i++) { 
                
                
              }
        }

        return (
            <botton  className="text-green" onClick={Validacion}><b>Solicitar Amistad</b></botton>
        );
    
}

export default Btn;